# Application Configuration
spring.application.name=ai-news-desk

# Server Configuration
server.port=8080
server.servlet.context-path=/api

# Database Configuration - MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/ai_news_desk?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=11411141a
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.show-sql=false

# JWT Configuration
spring.jwt.secret=your-secret-key-make-it-long-and-random-at-least-32-characters-for-security
spring.jwt.expiration=86400000

# Logging Configuration
logging.level.root=INFO
logging.level.com.ainewsdesk=DEBUG